<Window x:Class="MassageSaas.Cs.Views.RechargeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="会员充值"
        Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Text="充值金额" Margin="0,0,0,4" />
        <TextBox Grid.Row="2" x:Name="AmountBox" Padding="6" Margin="0,0,0,12" Text="100" />

        <TextBlock Grid.Row="3" Text="赠送金额" Margin="0,0,0,4" />
        <TextBox Grid.Row="4" x:Name="BonusBox" Padding="6" Margin="0,0,0,12" Text="0" />

        <TextBlock Grid.Row="5" Text="支付方式" Margin="0,0,0,4" />
        <ComboBox Grid.Row="6" x:Name="PayBox" Padding="6" Margin="0,0,0,12" SelectedIndex="0">
            <ComboBoxItem Content="现金" Tag="Cash" />
            <ComboBoxItem Content="微信" Tag="Wechat" />
            <ComboBoxItem Content="支付宝" Tag="Alipay" />
            <ComboBoxItem Content="银行卡" Tag="BankCard" />
        </ComboBox>

        <TextBlock Grid.Row="7" Text="备注" Margin="0,0,0,4" />
        <TextBox Grid.Row="8" x:Name="RemarkBox" Padding="6" Margin="0,0,0,12"
                 AcceptsReturn="True" TextWrapping="Wrap" />

        <Border Grid.Row="9" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="确认充值" Padding="16,6" Background="#2D6A4F" Foreground="White" BorderThickness="0" Click="Confirm_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
