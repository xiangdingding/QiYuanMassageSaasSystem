<Window x:Class="MassageSaas.Cs.Views.MemberTransferWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="转赠"
        Height="560" Width="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <DockPanel Margin="20">
        <Border DockPanel.Dock="Bottom" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="确认转赠" Padding="16,6" Background="#E6A23C" Foreground="White" BorderThickness="0" Click="Confirm_Click" />
            </StackPanel>
        </Border>

        <StackPanel>
            <TextBlock x:Name="HeaderText" FontWeight="Bold" FontSize="15" Margin="0,0,0,10" />

            <DockPanel Margin="0,0,0,10">
                <TextBlock Text="当前余额" Width="80" VerticalAlignment="Center" Foreground="#666" />
                <TextBlock x:Name="BalanceText" Foreground="#D9534F" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" />
                <TextBlock Text="将一并转走" Foreground="#999" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0" />
            </DockPanel>

            <TextBlock Text="转赠对象" Margin="0,0,0,4" Foreground="#666" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <RadioButton x:Name="ModeExisting" Content="已有会员" GroupName="Mode" IsChecked="True"
                             Checked="Mode_Changed" Margin="0,0,16,0" />
                <RadioButton x:Name="ModeNew" Content="新建会员" GroupName="Mode" Checked="Mode_Changed" />
            </StackPanel>

            <!-- 已有会员：搜索 + 候选单选 -->
            <StackPanel x:Name="ExistingPanel">
                <DockPanel Margin="0,0,0,6">
                    <Button DockPanel.Dock="Right" Content="查找" Padding="12,0" Margin="6,0,0,0" Click="Search_Click" />
                    <TextBox x:Name="QueryBox" Height="34" Padding="8,0" VerticalContentAlignment="Center"
                             AutomationProperties.Name="目标会员卡号或手机号">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" />
                        </TextBox.InputBindings>
                    </TextBox>
                </DockPanel>
                <ListBox x:Name="CandidateList" Height="150" AutomationProperties.Name="候选目标会员">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding CardNo, Mode=OneWay}" FontWeight="SemiBold" />
                                <Run Text="（" /><Run Text="{Binding Name, TargetNullValue='未填', Mode=OneWay}" />
                                <Run Text=" / " /><Run Text="{Binding Phone, Mode=OneWay}" /><Run Text="）" />
                                <Run Text="  余额 ¥" /><Run Text="{Binding Balance, StringFormat='F2', Mode=OneWay}" Foreground="#D9534F" />
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

            <!-- 新建会员 -->
            <StackPanel x:Name="NewPanel" Visibility="Collapsed">
                <TextBlock Text="新会员卡号" Margin="0,0,0,4" Foreground="#666" />
                <TextBox x:Name="NewCardNoBox" AutomationProperties.Name="新会员卡号" Height="34" Padding="8,0" Margin="0,0,0,8" VerticalContentAlignment="Center" />
                <TextBlock Text="手机号" Margin="0,0,0,4" Foreground="#666" />
                <TextBox x:Name="NewPhoneBox" AutomationProperties.Name="手机号" Height="34" Padding="8,0" Margin="0,0,0,8" VerticalContentAlignment="Center" />
                <TextBlock Text="姓名（可选）" Margin="0,0,0,4" Foreground="#666" />
                <TextBox x:Name="NewNameBox" AutomationProperties.Name="姓名" Height="34" Padding="8,0" Margin="0,0,0,8" VerticalContentAlignment="Center" />
            </StackPanel>

            <TextBlock Text="原因" Margin="0,8,0,4" Foreground="#666" />
            <TextBox x:Name="ReasonBox" AutomationProperties.Name="转赠原因" Height="40" Padding="6" Margin="0,0,0,10"
                     AcceptsReturn="True" TextWrapping="Wrap" MaxLength="200" />

            <Border Background="#FDF6EC" BorderBrush="#F5DAB1" BorderThickness="1" CornerRadius="3" Padding="8">
                <TextBlock Text="转赠后原卡余额清零并关闭，目标卡余额累加（不计入对方累计充值，不会因此升级）。"
                           Foreground="#B88230" FontSize="12" TextWrapping="Wrap" />
            </Border>
        </StackPanel>
    </DockPanel>
</Window>
