<UserControl x:Class="MassageSaas.Cs.Views.MemberPackagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="White" CornerRadius="4" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock DockPanel.Dock="Left" Text="会员套餐" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Text="状态" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <ComboBox SelectedIndex="{Binding StatusFilter, Mode=TwoWay}" Width="110" Height="30"
                              AutomationProperties.Name="按状态筛选">
                        <ComboBoxItem Content="全部" />
                        <ComboBoxItem Content="生效中" />
                        <ComboBoxItem Content="已用完" />
                        <ComboBoxItem Content="已过期" />
                        <ComboBoxItem Content="已作废" />
                    </ComboBox>
                    <Button Content="发卡" Padding="12,6" Margin="12,0,0,0" Background="#2D6A4F" Foreground="White" BorderThickness="0" Command="{Binding CreateCommand}" />
                    <Button Content="刷新" Padding="12,6" Margin="8,0,0,0" Command="{Binding ReloadCommand}" />
                </StackPanel>
            </DockPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#FAFAFA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="会员" Binding="{Binding MemberName}" Width="120" />
                    <DataGridTextColumn Header="套餐名" Binding="{Binding Title}" Width="180" />
                    <DataGridTextColumn Header="类型" Binding="{Binding Kind}" Width="80" />
                    <DataGridTextColumn Header="绑定服务" Binding="{Binding ServiceName}" Width="140" />
                    <DataGridTextColumn Header="实付" Binding="{Binding PaidAmount, StringFormat='¥{0:F2}'}" Width="90" />
                    <DataGridTextColumn Header="剩余/总次" Binding="{Binding RemainCount}" Width="80" />
                    <DataGridTextColumn Header="总次" Binding="{Binding TotalCount}" Width="60" />
                    <DataGridTextColumn Header="到期" Binding="{Binding ExpiresAt, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110" />
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80" />
                    <DataGridTemplateColumn Header="操作" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Margin="2" Padding="6,2" Content="作废" Background="#D9534F" Foreground="White" BorderThickness="0"
                                        Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
