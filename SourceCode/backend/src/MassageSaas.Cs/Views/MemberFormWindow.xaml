<Window x:Class="MassageSaas.Cs.Views.MemberFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="会员资料"
        Height="520" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="卡号" Margin="0,0,0,4" />
        <TextBox Grid.Row="1" x:Name="CardNoBox" Padding="6" Margin="0,0,0,12" />

        <TextBlock Grid.Row="2" Text="手机号" Margin="0,0,0,4" />
        <TextBox Grid.Row="3" x:Name="PhoneBox" Padding="6" Margin="0,0,0,12" />

        <TextBlock Grid.Row="4" Text="姓名" Margin="0,0,0,4" />
        <TextBox Grid.Row="5" x:Name="NameBox" Padding="6" Margin="0,0,0,12" />

        <Grid Grid.Row="6" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="折扣" Margin="0,0,0,4" />
                <TextBox x:Name="DiscountBox" Padding="6" Text="1.0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock Text="性别" Margin="0,0,0,4" />
                <ComboBox x:Name="GenderBox" Padding="6">
                    <ComboBoxItem Content="" />
                    <ComboBoxItem Content="男" />
                    <ComboBoxItem Content="女" />
                </ComboBox>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="7" Text="初始充值（仅开卡）" Margin="0,0,0,4" />
        <TextBox Grid.Row="8" x:Name="InitialBalanceBox" Padding="6" Margin="0,0,0,12" Text="0" />

        <Border Grid.Row="10" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="保存" Padding="16,6" Background="#2D6A4F" Foreground="White" BorderThickness="0" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
