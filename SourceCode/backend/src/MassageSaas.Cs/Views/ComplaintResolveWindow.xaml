<Window x:Class="MassageSaas.Cs.Views.ComplaintResolveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="处理投诉"
        Height="440" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FEF3C7" CornerRadius="3" Padding="10" Margin="0,0,0,12">
            <TextBlock x:Name="SummaryText" Foreground="#92400E" TextWrapping="Wrap" />
        </Border>

        <TextBlock Grid.Row="1" Text="处理方式" Margin="0,0,0,4" />
        <ComboBox Grid.Row="2" x:Name="ResolutionBox" Height="32" Margin="0,0,0,12"
                  SelectionChanged="ResolutionBox_SelectionChanged">
            <ComboBoxItem Content="改派给其他技师" Tag="Reassigned" />
            <ComboBoxItem Content="退款（记录用，退款另行操作）" Tag="Refunded" />
            <ComboBoxItem Content="口头道歉 / 补偿" Tag="Apologized" />
            <ComboBoxItem Content="不予处理" Tag="NoAction" />
        </ComboBox>

        <TextBlock Grid.Row="3" x:Name="TechLabel" Text="改派至技师" Margin="0,0,0,4" />
        <ComboBox Grid.Row="4" x:Name="TechBox" Height="32" Margin="0,0,0,12"
                  DisplayMemberPath="Name" AutomationProperties.Name="改派至技师" />

        <TextBlock Grid.Row="5" Text="处理说明" Margin="0,0,0,4" />
        <TextBox Grid.Row="6" x:Name="NoteBox" Padding="6" Height="70" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12" />

        <Border Grid.Row="8" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="确认处理" Padding="16,6" Background="#2D6A4F" Foreground="White" BorderThickness="0" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
