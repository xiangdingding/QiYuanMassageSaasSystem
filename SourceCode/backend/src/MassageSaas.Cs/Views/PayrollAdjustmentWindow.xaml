<Window x:Class="MassageSaas.Cs.Views.PayrollAdjustmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增奖金 / 扣款"
        Height="390" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="UserLabel" FontWeight="SemiBold" Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Text="类型" Margin="0,0,0,4" Foreground="#606266" />
        <ComboBox Grid.Row="2" x:Name="KindBox" Margin="0,0,0,12" Style="{StaticResource FilterCombo}" AutomationProperties.Name="奖扣类型">
            <ComboBoxItem Content="奖金（加项）" Tag="Bonus" />
            <ComboBoxItem Content="扣款（减项）" Tag="Deduction" />
        </ComboBox>

        <TextBlock Grid.Row="3" Text="金额（填正数）" Margin="0,0,0,4" Foreground="#606266" />
        <TextBox Grid.Row="4" x:Name="AmountBox" Margin="0,0,0,12" Style="{StaticResource FieldText}" AutomationProperties.Name="金额" />

        <TextBlock Grid.Row="5" Text="原因" Margin="0,0,0,4" Foreground="#606266" />
        <TextBox Grid.Row="6" x:Name="ReasonBox" Height="56" VerticalAlignment="Top" Style="{StaticResource FieldTextMultiline}" AutomationProperties.Name="原因" />

        <Border Grid.Row="7" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="20,0" Margin="0,0,8,0" AutomationProperties.Name="取消" Click="Cancel_Click" Style="{StaticResource GhostBtn}" />
                <Button Content="保存" Padding="20,0" AutomationProperties.Name="保存" Click="Save_Click" Style="{StaticResource PrimaryBtn}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
