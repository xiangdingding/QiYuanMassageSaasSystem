<Window x:Class="MassageSaas.Cs.Views.ScheduleFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增班次"
        Height="430" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="技师" Margin="0,0,0,4" />
        <ComboBox Grid.Row="1" x:Name="UserBox" Height="32" Margin="0,0,0,12"
                  DisplayMemberPath="Name" AutomationProperties.Name="选择技师" />

        <TextBlock Grid.Row="2" Text="日期" Margin="0,0,0,4" />
        <DatePicker Grid.Row="3" x:Name="DateBox" Margin="0,0,0,12" />

        <TextBlock Grid.Row="4" Text="开始时间（如 09:00）" Margin="0,0,0,4" />
        <TextBox Grid.Row="5" x:Name="StartBox" Padding="6" Margin="0,0,0,12" Text="09:00" />

        <TextBlock Grid.Row="6" Text="结束时间（如 21:00）" Margin="0,0,0,4" />
        <TextBox Grid.Row="7" x:Name="EndBox" Padding="6" Margin="0,0,0,12" Text="21:00" />

        <TextBlock Grid.Row="8" Text="备注" Margin="0,0,0,4" />
        <TextBox Grid.Row="9" x:Name="RemarkBox" Padding="6" Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12" />

        <Border Grid.Row="11" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="16,6" Margin="0,0,8,0" Click="Cancel_Click" />
                <Button Content="保存" Padding="16,6" Background="#2D6A4F" Foreground="White" BorderThickness="0" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
