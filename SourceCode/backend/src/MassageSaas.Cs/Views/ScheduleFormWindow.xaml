<Window x:Class="MassageSaas.Cs.Views.ScheduleFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增排班"
        Height="540" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="员工" Margin="0,0,0,4" Foreground="#666" />
        <ComboBox Grid.Row="1" x:Name="UserBox" Margin="0,0,0,12"
                  AutomationProperties.Name="选择员工"
                  Style="{StaticResource FilterCombo}">
            <ComboBox.ItemTemplate>
                <DataTemplate><TextBlock Text="{Binding Display}" /></DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="2" Text="日期" Margin="0,0,0,4" Foreground="#666" />
        <DatePicker Grid.Row="3" x:Name="DateBox" Margin="0,0,0,12"
                    AutomationProperties.Name="日期"
                    PreviewMouseLeftButtonUp="DatePicker_PreviewMouseUp"
                    SelectedDateFormat="Short"
                    Style="{StaticResource FilterDatePicker}" />

        <TextBlock Grid.Row="4" Text="开始时间（可输入或下拉选择，如 09:00）" Margin="0,0,0,4" Foreground="#666" />
        <ComboBox Grid.Row="5" x:Name="StartBox" Margin="0,0,0,12"
                  AutomationProperties.Name="开始时间" Style="{StaticResource FieldComboEditable}" />

        <TextBlock Grid.Row="6" Text="结束时间（可输入或下拉选择，如 21:00）" Margin="0,0,0,4" Foreground="#666" />
        <ComboBox Grid.Row="7" x:Name="EndBox" Margin="0,0,0,12"
                  AutomationProperties.Name="结束时间" Style="{StaticResource FieldComboEditable}" />

        <TextBlock Grid.Row="8" Text="备注" Margin="0,0,0,4" Foreground="#666" />
        <TextBox Grid.Row="9" x:Name="RemarkBox" Height="50" Margin="0,0,0,12" MaxLength="200"
                 AutomationProperties.Name="备注" Style="{StaticResource FieldTextMultiline}" />

        <Border Grid.Row="11" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="20,0" Margin="0,0,8,0" AutomationProperties.Name="取消"
                        Click="Cancel_Click" Style="{StaticResource GhostBtn}" />
                <Button Content="保存" Padding="20,0" AutomationProperties.Name="保存"
                        Click="Save_Click" Style="{StaticResource PrimaryBtn}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
