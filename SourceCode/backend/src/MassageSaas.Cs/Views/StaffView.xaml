<UserControl x:Class="MassageSaas.Cs.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="White" CornerRadius="4" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock DockPanel.Dock="Left" Text="员工管理" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Text="角色：" VerticalAlignment="Center" />
                    <ComboBox Width="120" SelectedValuePath="Tag" SelectedValue="{Binding RoleFilter}">
                        <ComboBoxItem Content="全部" Tag="" />
                        <ComboBoxItem Content="店主" Tag="ShopOwner" />
                        <ComboBoxItem Content="店长" Tag="StoreManager" />
                        <ComboBoxItem Content="收银员" Tag="Cashier" />
                        <ComboBoxItem Content="技师" Tag="Technician" />
                    </ComboBox>
                    <TextBox Width="160" Margin="8,0,0,0" Padding="6"
                             Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"
                             AutomationProperties.Name="搜索框" />
                    <Button Content="搜索" Padding="12,6" Margin="8,0,0,0" Command="{Binding ReloadCommand}" />
                    <Button Content="新建" Padding="12,6" Margin="8,0,0,0" Background="#2D6A4F" Foreground="White" BorderThickness="0" Command="{Binding CreateCommand}" />
                </StackPanel>
            </DockPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#FAFAFA"
                      AutomationProperties.Name="员工列表">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="AutomationProperties.Name">
                            <Setter.Value>
                                <MultiBinding StringFormat="工号 {0}，{1}，{2}，{3}">
                                    <Binding Path="EmployeeNo" FallbackValue="无" />
                                    <Binding Path="RealName" FallbackValue="" />
                                    <Binding Path="Role" />
                                    <Binding Path="IsActive" />
                                </MultiBinding>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="工号" Binding="{Binding EmployeeNo}" Width="60" />
                    <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="140" />
                    <DataGridTextColumn Header="姓名" Binding="{Binding RealName}" Width="120" />
                    <DataGridTextColumn Header="手机号" Binding="{Binding Phone}" Width="120" />
                    <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="100" />
                    <DataGridTextColumn Header="盲人" Binding="{Binding IsBlind}" Width="60" />
                    <DataGridTextColumn Header="启用" Binding="{Binding IsActive}" Width="60" />
                    <DataGridTextColumn Header="最近登录" Binding="{Binding LastLoginAt, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160" />
                    <DataGridTemplateColumn Header="操作" Width="240">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Margin="2" Padding="6,2" Content="编辑"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                    <Button Margin="2" Padding="6,2" Content="重置密码"
                                            Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
