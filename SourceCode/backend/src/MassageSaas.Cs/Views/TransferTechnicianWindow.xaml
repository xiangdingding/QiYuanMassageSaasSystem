<Window x:Class="MassageSaas.Cs.Views.TransferTechnicianWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="转钟（更换技师）"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2D6A4F" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#245A41" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#9CC3B0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="#D1D5DB" BorderThickness="1"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#F3F4F6" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="#2D6A4F" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" Text="项目" Width="84" FontSize="14" Foreground="#666" VerticalAlignment="Center" />
            <TextBlock x:Name="ServiceText" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
        </DockPanel>
        <DockPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" Text="原技师" Width="84" FontSize="14" Foreground="#666" VerticalAlignment="Center" />
            <TextBlock x:Name="OldTechText" FontSize="14" VerticalAlignment="Center" />
        </DockPanel>
        <DockPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock DockPanel.Dock="Left" Text="新技师" Width="84" FontSize="14" Foreground="#666" VerticalAlignment="Center" />
            <ComboBox x:Name="TechCombo" Height="38" VerticalContentAlignment="Center"
                      SelectionChanged="Tech_SelectionChanged"
                      AutomationProperties.Name="选择新技师">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding EmployeeNo, FallbackValue='-', Mode=OneWay}" />
                            <Run Text=" · " />
                            <Run Text="{Binding RealName, TargetNullValue='', Mode=OneWay}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </DockPanel>
        <DockPanel Grid.Row="3" Margin="0,0,0,18">
            <TextBlock DockPanel.Dock="Left" Text="原因" Width="84" FontSize="14" Foreground="#666" VerticalAlignment="Top" Margin="0,8,0,0" />
            <TextBox x:Name="ReasonBox" Height="56" Padding="8" FontSize="14"
                     AcceptsReturn="True" TextWrapping="Wrap" MaxLength="200"
                     AutomationProperties.Name="转钟原因，可选"
                     AutomationProperties.HelpText="如：客人要求换人 / 原技师有事" />
        </DockPanel>

        <Border Grid.Row="4" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="0,14,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Padding="22,8" Margin="0,0,10,0"
                        Style="{StaticResource GhostButton}" Click="Cancel_Click"
                        AutomationProperties.Name="取消转钟" />
                <Button x:Name="ConfirmButton" Content="确认转钟" Padding="24,8" IsEnabled="False"
                        Style="{StaticResource PrimaryButton}" FontWeight="Bold" Click="Confirm_Click"
                        AutomationProperties.Name="确认转钟" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
