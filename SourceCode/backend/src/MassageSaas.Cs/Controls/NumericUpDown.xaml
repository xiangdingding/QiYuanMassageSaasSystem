<UserControl x:Class="MassageSaas.Cs.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!--  左右步进按钮：扁平、悬停浅灰、绿字  -->
        <Style x:Key="StepBtn" TargetType="RepeatButton">
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E6F0EB" />
                                <Setter Property="Foreground" Value="#2D6A4F" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#F3F4F6" />
                                <Setter Property="Foreground" Value="#C4C7CC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="Bd"
            Height="34"
            Background="White"
            BorderBrush="#D1D5DB"
            BorderThickness="1"
            CornerRadius="6"
            SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="34" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="34" />
            </Grid.ColumnDefinitions>

            <RepeatButton x:Name="MinusBtn"
                          Grid.Column="0"
                          AutomationProperties.Name="减少"
                          Click="Minus_Click"
                          Content="−"
                          Style="{StaticResource StepBtn}" />
            <Border Grid.Column="0"
                    Width="1"
                    HorizontalAlignment="Right"
                    Background="#E5E7EB" />

            <TextBox x:Name="ValueBox"
                     Grid.Column="1"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     Background="Transparent"
                     BorderThickness="0"
                     FontSize="14"
                     Foreground="#374151"
                     LostFocus="ValueBox_LostFocus"
                     PreviewKeyDown="ValueBox_PreviewKeyDown" />

            <Border Grid.Column="2"
                    Width="1"
                    HorizontalAlignment="Left"
                    Background="#E5E7EB" />
            <RepeatButton x:Name="PlusBtn"
                          Grid.Column="2"
                          AutomationProperties.Name="增加"
                          Click="Plus_Click"
                          Content="+"
                          Style="{StaticResource StepBtn}" />
        </Grid>
    </Border>
</UserControl>
