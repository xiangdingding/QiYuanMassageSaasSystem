# 上线前人工补充清单

> 本清单列出**代码层面无法替代、需要人工完成**的事项：账号凭证、基础设施、硬件、环境与决策。
> 系统功能开发已完成（后端 API / CS 收银端 / BS 管理端 / 平台端 / 小程序 + 5 类外设串口驱动），以下为投产准备工作。
>
> 最后更新：2026-05-21

## 责任角色说明

| 代号 | 角色 | 职责范围 |
|---|---|---|
| PM | 项目经理 | 统筹、决策、对外协调 |
| 商务 | 商务 / 财务 | 第三方账号申请、资质材料、合同 |
| 后端 | 后端开发 | API 配置、集成、密钥写入 |
| 小程序 | 小程序开发 | 微信小程序端配置与发布 |
| 运维 | 运维 / 系统管理 | 服务器、数据库、部署、监控 |
| QA | 测试 | 验收测试 |
| 店长 | 业务方 / 店长 | 门店业务数据录入 |
| 硬件 | 硬件供应商 / 现场实施 | 外设安装与驱动协议 |

> 工时为**有效人时估算**（h = 人时，d = 人天 = 8h），不含第三方审核等待周期（在备注中单列）。

## 一、第三方账号与凭证

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 注册微信小程序，取得 AppId / AppSecret 并填入 `appsettings`（`Notifications:WeChat`） | 商务 · 后端 | 商务 0.5d ＋ 后端 0.5h | 微信审核约 1-3 工作日 |
| ☐ 申请 4 个订阅消息模板（生日 / 到期 / 预约 / 充值），模板 ID 填入 `Notifications:WeChat:Templates` | 后端 | 2h | 每模板审核约 1-7 天 |
| ☐ 把 `project.config.json` 的 `appid` 改成正式 appid | 小程序 | 0.2h | |
| ☐ 微信后台「开发设置」把后端 API 域名加入 request 合法域名（HTTPS） | 运维 | 0.5h | 依赖域名与证书就绪 |
| ☐ 申请微信支付 V3：商户号、API 证书、APIv3 密钥并配置 | 商务 · 后端 | 商务 1d ＋ 后端 1h | 商户号审核数个工作日 |
| ☐ 申请支付宝应用：AppId、应用私钥 / 支付宝公钥并配置 | 商务 · 后端 | 商务 0.5d ＋ 后端 1h | |
| ☐ 配置公网可达的支付回调 URL，确认在 `TenantStatusMiddleware` 白名单内 | 后端 | 0.5h | |
| ☐ 短信服务商账号、签名、模板（如启用短信通知，可选） | 商务 · 后端 | 商务 0.5d ＋ 后端 1h | 可选 |

## 二、基础设施

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 部署 MySQL 8.0，建库 `massage_saas`，连接串写入 `appsettings.Development.json` 或环境变量 | 运维 | 2h | 勿入库 |
| ☐ 部署 Redis 7 | 运维 | 1h | P1 阶段起必需 |
| ☐ 服务器 / 域名 / 反向代理 / HTTPS 证书 | 运维 | 1d | |

## 三、安全（务必处理）

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 生产环境 JWT 签名密钥换成强随机值，不沿用仓库默认值 | 后端 | 0.5h | |
| ☐ 首次启动 seed 的默认管理员 `admin / admin@123` 登录后立即改密码 | PM · 运维 | 0.2h | 高优先 |
| ☐ AutoMapper 12.0.1 漏洞（GHSA-rvv3-g6hj-g44x）评估是否升级 | 后端 · PM | 1h | 升级若执行另计，有破坏性变更 |

## 四、硬件对接

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 接小票打印机，改 `MassageSaas.Cs/appsettings.json` 的 `Printer` 节，用「测试打印小票」核对出纸/切纸/钱箱 | 硬件 · 运维 | 2h / 店 | 现场作业，按门店计 |
| ☐ 客显 / 来电盒 / 磁条读卡器现场接线 + 改 CS 端 `appsettings.json` 对应节 `enabled=true` | 硬件 · 运维 | 每种 0.5h / 店 | 串口驱动已落地（`Services/Devices/Serial/`），按 RS232 标准协议工作；HID 键盘式读卡器无需配置 |

## 五、部署与运维

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 把 `/health` 接入监控系统（Uptime Kuma / Prometheus 等） | 运维 | 2h | |
| ☐ 配置 Serilog 日志归集与轮转保留策略 | 运维 | 3h | 日志写 `logs/` 目录 |
| ☐ 制定数据库备份与恢复方案 | 运维 | 0.5d | |
| ☐ 为 `Testcontainers.MySql` 端到端测试准备 Docker 环境 | 运维 | 2h | InMemory 集成测试已可直接跑 |

## 六、业务初始化数据

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 确认套餐定价（DbInitializer 已 seed 基础/标准/旗舰三档） | PM · 商务 | 1h | |
| ☐ 各租户开店后录入门店、服务项目、房间、技师档案、提成规则、薪资档案 | 店长 | 0.5-1d / 店 | 技师档案含盲人资质证号、钟数上限 |

## 七、验收测试（需真机 / 真人）

| 事项 | 责任角色 | 预估工时 | 备注 |
|---|---|---|---|
| ☐ 小程序无障碍真机实测（字号、aria-label、金额朗读） | QA | 0.5d | 配读屏软件 |
| ☐ CS 端无障碍实测（全键盘可达、控件朗读） | QA | 0.5d | NVDA / Windows 讲述人 |
| ☐ 微信通知闭环实测（绑卡 → 充值 → 收到订阅消息） | QA · 后端 | 2h | 依赖模板配置完成 |
| ☐ 支付闭环实测（真实小额支付 + 回调幂等） | QA · 后端 | 3h | 依赖支付凭证就绪 |

## 工时汇总（粗估）

| 类别 | 估算 | 说明 |
|---|---|---|
| 一次性工程 + 配置 + 验收 | 约 8 人天 | 不含按店项、不含第三方审核 |
| 每新增门店 | 约 1 人天 / 店 | 打印机 + 客显 + 来电盒 + 磁条卡现场接线 + 业务数据录入 |
| 第三方审核周期 | 另计 | 小程序、订阅消息模板、支付商户号均有数日审核 |

> 建议排期：先行启动有审核周期的第一部分（微信、支付账号申请），与基础设施、安全配置并行推进。
